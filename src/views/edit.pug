extends base

block content 
    h4 Edit title of video
    form(method="POST")  
        input(name="title", placeholder="Video Title", value=selectedVideo.title, required)
        input(type="submit", value="Save")